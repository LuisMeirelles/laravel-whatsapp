info:
  name: Get Endpoint Request Error Rate Metric
  type: http
  seq: 3

http:
  method: GET
  url: https://graph.facebook.com/{{Version}}/{{Flow-ID}}?fields=metric.name(ENDPOINT_REQUEST_ERROR_RATE).granularity(LIFETIME).since(2024-01-28).until(2024-01-30)
  params:
    - name: fields
      value: metric.name(ENDPOINT_REQUEST_ERROR_RATE).granularity(LIFETIME).since(2024-01-28).until(2024-01-30)
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Get Endpoint Request Error Rate Metric
    request:
      url: https://graph.facebook.com/{{Version}}/{{Flow-ID}}?fields=metric.name(ENDPOINT_REQUEST_ERROR_RATE).granularity(LIFETIME).since(2024-01-28).until(2024-01-30)
      method: GET
      params:
        - name: fields
          value: metric.name(ENDPOINT_REQUEST_ERROR_RATE).granularity(LIFETIME).since(2024-01-28).until(2024-01-30)
          type: query
          description: Return specific fields in the response
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
              "id": "flow-1",
              "metric": {
                  "granularity": "LIFETIME",
                  "name": "ENDPOINT_REQUEST_ERROR_RATE",
                  "data_points": [
                      {
                          "timestamp": "2024-01-28T08:00:00+0000",
                          "data": [
                              {
                                  "key": "value",
                                  "value": 0.12625250501002
                              }
                          ]
                      }
                  ]
              }
          }

docs: Can request specific fields by enabling the \`fields\` query param
