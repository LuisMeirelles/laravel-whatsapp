info:
  name: Delete Flow
  type: http
  seq: 6

http:
  method: DELETE
  url: https://graph.facebook.com/{{Version}}/{{Flow-ID}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Delete Flow
    request:
      url: https://graph.facebook.com/{{Version}}/{{Flow-ID}}
      method: DELETE
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
              "success": true
          }

docs: Deletes the flow entirely. This action is not reversible. Only a DRAFT flow can be deleted.
