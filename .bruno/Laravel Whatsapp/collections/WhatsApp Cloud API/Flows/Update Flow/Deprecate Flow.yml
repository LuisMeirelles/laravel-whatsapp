info:
  name: Deprecate Flow
  type: http
  seq: 5

http:
  method: POST
  url: https://graph.facebook.com/{{Version}}/{{Flow-ID}}/deprecate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Deprecate Flow
    request:
      url: https://graph.facebook.com/{{Version}}/{{Flow-ID}}/publish
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
              "success": true
          }

docs: Updates the status of the flow as "DEPRECATED". This action is not reversible. Only a published flow can be deprecated to prevent sending or opening it.
